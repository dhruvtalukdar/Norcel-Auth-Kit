## Profile Page -
	> Do we need the update password functionality if logged in using google auth?
Admin Panel Page - 
	> Last Sign is coming as “never” even though it was signed it (when used google login)
	> 


## Security Log Page - 
	> You don’t have permission to access the page(this came first ) then when tried to access again the below error came 
	> / ERROR
	We couldn't load this page.
Invalid `prisma.role.findMany()` invocation: Timed out fetching a new connection from the connection pool. More info: http://pris.ly/d/connection-pool (Current connection pool timeout: 10, connection limit: 1)


## Active Sessions Page - 
	> Currency coming as : Unknown browser on Unknown OS (Unknown device)
Last active 4 minutes ago
		Can we actually log the browser/device details ? Like mac , safari;

## Overall - 
	> When logging out – the home page doesn’t render the content, need to refresh for it to load
	> Navigation is really slow, when changing tab, it takes a lot of time
	> Settings and active sessions tab got highlighted together when I clicked the active sessions tab
	> Same happened with Admin Panel & User when clicked on Users tab
